package ${PACKAGE_PATH};

import ${BASE_PATH}.BasePresenter;
import ${BASE_PATH}.BaseView;

public class ${CLASS_NAME}Contract {
    interface View extends BaseView<Present> {

    }

    interface Present extends BasePresenter {

    }
}